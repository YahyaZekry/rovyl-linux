# Session Log

> Part of Rovyl/.project-knowledge/ | Last updated: 2026-09-14

| Date | Summary |
|------|---------|
| 2026-09-14 | Scanned codebase, mapped every Windows touchpoint in `electron-main.js` (~55-65% coupled), produced Linux port plan (5 phases), created `.project-knowledge/` |
| 2026-09-14 | Linux port phases 1–3 + packaging config: platform seams, `rovyl-helper-linux` (C, X11 grabs + XTest, same protocol), `linux-desktop.cjs` (discovery/icons/launch/terminal), app boots on Linux with helpers + trigger armed + discovery verified; all smoke tests pass; deb/AppImage target added. Remaining: real-gesture E2E on X11, dist run, Wayland fallback messaging |
| 2026-09-14 | Wayland research (Exa) → `docs/wayland-port-plan.md` incl. CI/DE-test workflows; Wayland made primary (no X11 machine). Electron 28→44.3.0 upgraded; diagnosed + fixed the native-Wayland boot stall (software rendering kills first paint of transparent windows — ready-to-show never fires); app now boots fully natively on Wayland (window, shortcut, trigger armed) |
| 2026-09-14 | Wayland gesture shipped: `mouse-blocker-evdev` mode in the C helper (evdev `EVIOCGRAB` + uinput reinject, capabilities/identity copied), `POS x y` protocol extension (main feeds cursor position while BLOCK is live — evdev has no absolute position), clickless capture gated off on Wayland (no warp). Found + fixed a 9-token BLOCK parse bug that broke BLOCK in the X11 mode too. Full virtual-device E2E suite green; app runs on Wayland with the helper live |
| 2026-09-14 | First real-grab attempt froze the pointer (REL forwarded as EV_KEY); fixed, added REL assertions to the E2E suite, app relaunched on Wayland with the corrected helper — gesture live for acceptance testing |
| 2026-09-14 | UX fixes from live testing: BLOCK_CLICK (click-away closes wheel/panel, via helper→main→renderer), unified the click/hold boundary at 1 s (helper threshold was 400 ms so slower menu clicks landed in apps). FirstRun overlay pinned to viewport. All verified with the virtual-device rig |
| 2026-09-14 | Helper-respawn self-healing added after my test cleanup killed the live helper (menu dead, log silent). Exit handler → requestBlockerRespawn → re-arm; kill-and-respawn verified live |
| 2026-09-14 | Autoscroll root cause: KDE per-device setting on the forwarded pointer (user fixed by enabling it there). Cleaned leaked test devices; helper now skips all rovyl-* named devices |
| 2026-09-14 | README updated for the dual-platform fork (badges, Linux install/build/quirks sections, Electron 44, Wayland capability notes) |
| 2026-09-14 | Second force-quit report → block watchdog added (5 s stdin-silence auto-unblock, verified live); close-gesture releases the block earlier; user announces Linux-only fork rename (publish/repo field updates pending) |
| 2026-09-14 | Four bug fixes from live use: moniker-wrapped shortcuts (launch+icons dead), fake autostart on Linux (real xdg autostart now), "Start with Windows" label, taskbar entry on wheel open (Wayland skipTaskbar limitation — documented, needs layer-shell) |
| 2026-09-15 | First real install: deb + AppImage build green (deb maintainer field added), AppImage installed to ~/.local/bin with a menu entry. Found + fixed the packaged-only spawn ENOTDIR (unpacked-first helper resolution). Remaining stall on this machine = NVIDIA driver mismatch, needs reboot |
| 2026-09-15 | "Doesn't work" triaged: no reboot had happened (uptime 6 days, NVIDIA 610/615 mismatch live, yesterday's GPU-broken instance still running). Desktop entry now disables the sandbox, boot-stall diagnostic added, reinstalled |
| 2026-09-15 | Root cause fixed: Wayland cursor truth moved to the renderer (wheel-cursor IPC + heartbeat + fail-open), debug breadcrumbs stripped, hooks crash fixed, clean build reinstalled — user verified the full loop: wheel, icons, launches, autoscroll, click-away, autostart toggle |
| 2026-09-15 | Three-zone click thresholds (native <350ms / menu 350-1000 / autoscroll >1000) rejected by the user mid-test; reverted on main (fc02450), experiment preserved on the `three-zone-experiment` branch. Behavior restored to: quick MMB click = native action + menu together |
| 2026-09-18 | README recreated Linux-first (tested-on table, middle-click behaviour, Wayland limits). Idle invisible-layer bug fixed by hiding the window on Wayland. New setting: "Menu on middle click over apps" toggle. Follow-pointer documented as a Wayland limitation (no global cursor query); hold mode already exists in Settings for hold-to-open |
| 2026-09-18 | Follow pointer clamped on Wayland (picker shows limited-by-Wayland notice, main clamps to main screen; works on X11). Project folder renamed rovyl → rovyl-linux (compat symlink at the old path). Hold-mode open confirmed movement-independent |
| 2026-09-18 | CI + contribution templates: build matrix, tests, wayland-e2e (helper gesture rig committed under scripts/e2e with a two-fifo driver — the single-fifo version raced helper commands against gesture input), issue forms, PR template. test:gesture npm script added |
| 2026-09-18 | Upstream sync merged (v1.8→v1.14.0, 54 commits): two-window wheel architecture, cursor placement, custom icons, docks, area targeting. Wayland layer re-applied as 21 scripted ports onto the upstream base (hand-merge of the monolith degraded; scripted re-application was deterministic). All verification green (29 smokes, 10 units, gesture E2E); packaged 1.14.0 installed and live. Diag log moved to ~/.rovyl/ |
| 2026-09-18 | Tray fix for Linux: upstream tray never calls setContextMenu (Windows pop-up dance) and Linux StatusNotifier items get no click events — ItemIsMenu stayed false, so Settings/Quit were unreachable. Menu now attached permanently on Linux and rebuilt on state change; Windows keeps its pop-up flow |
| 2026-09-18 | Follow pointer + hotkey fixed on Wayland: helper now tracks an approximate cursor position (POS anchor + REL deltas, accel-drift acknowledged) and answers CURSORQ; main queries it before opening so placement/monitor use the real hand position. Global hotkey moved to passive keyboard observation in the helper (HOTKEY code+mask command, HOTKEY_PRESSED event) — Wayland gives globalShortcut no protocol. Wired through accelerator parse helpers |
| 2026-09-18 | Second full-mouse freeze after upstream sync — root cause: the merged close path never lifts the gesture block (upstream close code has no concept of it). Fixed: clearRadialMouseBlocking on every close-to-idle + watchdog tightened to 2s. Rebuilt, reinstalled |
| 2026-09-18 | Click-mode final design re-applied on the fixed base: helper holds a quick middle click 250ms (CLICK_CONSUMED cancels when the menu opens), wheel first-450ms shield absorbs late landings, close path always unblocks, 2s watchdog. E2E extended with consumed-click scenario + incremental assertions — all pass. Installed and running |
| 2026-09-19 | X11 parity: the hold-back/CLICK_CONSUMED machinery existed only in the evdev mode — added to the X11 mode too (pending state, delayed injection, cancel branch). User-reported symptoms traced: tab-click opened menu because consume ran but injection also fired on some paths; click-on-open-menu launched an app because the 450ms shield only lived in the pre-merge RadialMenu. Installed, armed |
| 2026-09-19 | Toggle "Menu on middle click over apps" was dead at runtime: the config-save path never synced cachedRadialFlags.middleClickOpensMenu (only startup hydration did) — fixed. "Start with Windows" label shown on Linux because waylandLimited depended on async IPC — now UA-based (isLinux). Knowledge reread; session/hist entries updated |
