# Brag Plan (Instagram variant): Rovyl for Linux

Variant of `../brag-output/brag-plan.md` with three deltas requested by the user:

1. **Format: vertical 9:16 — 1080x1920** (Instagram Reels/Story). Every scene is
   re-laid-out for portrait: hook text centered, wheel enlarged (radius 200, 104px
   tiles) at screen center, chips wrap 2x2, calibration meter narrowed to 900px,
   captions pinned to the lower third.
2. **No music.** The music bed, its volume automation, and the audio-reactive glow
   treatment (which sampled the track's bands) are all removed. SFX remain: mouse
   click on the press, soft drops on first/last tile pop, selection tick on the
   tooltip lock, bell on the launch payoff, soft rollovers on first/last chip, five
   click ticks on the calibration presses, soft impact on the threshold drop, bell
   on the logo. Beat-grid timings from the original plan are kept as the animation
   pacing grid (they no longer refer to audible music).
3. **New hook (user's line, verbatim):** "One gesture, every app on your Linux
   desktop." — "One gesture," slams first, "every app on your Linux desktop."
   completes the line, then the cursor presses.

Everything else — storyboard, tone (cinematic), duration (23s), scene order,
copy, visual identity, outro tagline "One gesture. Any destination." — carries
over unchanged from the master plan.
